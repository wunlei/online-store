export const BASE_URL_API = "https://dummyjson.com/";

export enum API_Endpoints {
  products = "products",
  search = "products/search",
  categories = "products/categories",
  category = "products/category",
}

export enum API_SearchParams {
  limit = "limit",
  skip = "skip",
}

export const LIMIT = 10;
